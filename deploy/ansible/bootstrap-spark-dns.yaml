# Deploy spark's dns-tunnel-server (ADR 0011) — the clean-slate DNS-tunnel transport, a drop-in
# modernization of Lantern's dnstt escalation tier. This mirrors
# getlantern/lantern-cloud `ans/bootstrap-dnstt.yaml` (same shape: a static binary + a systemd unit +
# a :53→:5300 DNAT), swapping dnstt's Noise keypair for spark's static Ed25519 identity. It is meant to
# be lifted into lantern-cloud/ans/ alongside the dnstt playbook once reviewed.
#
# Prereqs:
#   - inventory group `spark_dns` (see inventory/spark-dns.example.yaml)
#   - a delegated tunnel zone: `t.<domain>` NS → `ns-spark.<domain>` A → the host IP.
#     Use an UNATTRIBUTABLE domain, NOT a known Lantern domain — a censor's
#     resolver is far likelier to filter a recognizable name.
#   - the cross-built static server binary at files/dns-tunnel-server
#     (`cargo zigbuild --release -p dns-tunnel-server --target x86_64-unknown-linux-musl`;
#      use aarch64-unknown-linux-musl for ARM hosts). Static musl → distro-agnostic.
#   - the server's static private key at files/spark-dns.privkey — the base64 privkey line from
#     `dns-tunnel-server keygen` (secret; mode 0600). Distribute the matching `pubkey` to clients;
#     the private key never leaves the server.
#
# Run (replace the zone with your delegated one; example.com is RFC 2606 reserved and inert):
#   ansible-playbook -i inventory/spark-dns.yaml bootstrap-spark-dns.yaml -e zone=t.example.com
- name: Deploy and start spark dns-tunnel-server
  hosts: spark_dns
  become: true
  vars:
    bind: "0.0.0.0:5300"
    idle_secs: 300
    # Telemetry (issue #165). **On by default**, matching the binary's own `otlp` feature default
    # and for the same reason it gives: if reporting takes an extra flag, the likeliest outcome of
    # a redeploy is a server that silently reports nothing. A deployed server that isn't in SigNoz
    # is one we cannot tell apart from a blocked one.
    #
    # Pass the key:  -e otel_key=<key>   (and -e otel_instance=dns-2 to name it on a dashboard)
    # Opt out:       -e otel_host=""     (the exporter never starts; no outbound connections at all)
    #
    # Forgetting the key is a deploy failure rather than a silent no-telemetry server — see the
    # assert below. `otel_instance` is an opaque operator label; never derive it from the zone or
    # the host's address (the server's own hygiene rule).
    otel_host: "ingest.us.signoz.cloud"
    otel_key: ""
    otel_instance: "dns-tunnel"
    otel_port: 443
    otel_interval_secs: 60
  tasks:
    - name: Require the tunnel zone
      assert:
        that:
          - zone is defined
          - zone | length > 0
        fail_msg: "pass -e zone=t.<your-domain> (an unattributable domain, not a known Lantern one)"

    # Telemetry is on by default, so a missing key must stop the deploy rather than produce a
    # server that runs fine and reports nothing — which is the failure mode that leaves us unable
    # to tell a healthy deployment from a blocked one. Opting out is explicit and still allowed.
    - name: Require an ingestion key when telemetry is on
      assert:
        that:
          - otel_key | length > 0
        fail_msg: >-
          telemetry is on by default: pass -e otel_key=<signoz ingestion key>,
          or opt out explicitly with -e otel_host=""
      when: otel_host | length > 0

    - name: Config dir
      file:
        path: /etc/spark-dns
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Copy the dns-tunnel-server binary
      copy:
        src: files/dns-tunnel-server
        dest: /usr/local/bin/dns-tunnel-server
        mode: "0755"
      notify: Restart spark-dns

    - name: Write the OTLP ingestion key (secret — root-only)
      # A separate root-only file rather than an `Environment=` line in the unit: unit files are
      # world-readable and `systemctl show` prints their environment, either of which would expose
      # the key to any local user. `no_log` keeps it out of ansible's own output too.
      copy:
        content: "SPARK_OTEL_INGEST_KEY={{ otel_key }}\n"
        dest: /etc/spark-dns/otel.env
        owner: root
        group: root
        mode: "0600"
      no_log: true
      when: otel_host | length > 0 and otel_key | length > 0
      notify: Restart spark-dns

    - name: Remove the OTLP ingestion key when telemetry is off
      # Turning telemetry off must actually remove the secret, not leave it readable on disk for
      # whoever next gets root on the box.
      file:
        path: /etc/spark-dns/otel.env
        state: absent
      when: otel_host | length == 0 or otel_key | length == 0
      notify: Restart spark-dns

    - name: Copy the server private key (secret — root-only)
      copy:
        src: files/spark-dns.privkey
        dest: /etc/spark-dns/privkey
        owner: root
        group: root
        mode: "0600"
      notify: Restart spark-dns

    # Recursive resolvers always hit :53; the server runs unprivileged on :5300 and we redirect.
    #
    # This MUST survive reboot. Without persistence the failure is silent and total: systemd brings
    # the server back on :5300, it answers correctly if you query that port, and every recursive
    # resolver in the world still gets nothing — so the tier looks blocked rather than broken.
    - name: Install iptables-persistent so the redirect survives reboot
      apt:
        name: iptables-persistent
        state: present
        update_cache: true
      # Pre-seed the debconf prompts; the package otherwise blocks asking whether to save rules.
      environment:
        DEBIAN_FRONTEND: noninteractive

    - name: DNAT udp/53 → udp/5300
      iptables:
        table: nat
        chain: PREROUTING
        protocol: udp
        destination_port: "53"
        jump: REDIRECT
        to_ports: "5300"
        comment: spark-dns 53->5300
      notify: Save iptables rules

    - name: systemd unit
      template:
        src: templates/spark-dns.service.j2
        dest: /etc/systemd/system/spark-dns.service
      notify: Restart spark-dns

    - name: Enable and start spark-dns
      systemd:
        name: spark-dns
        enabled: true
        state: started
        daemon_reload: true

  handlers:
    - name: Restart spark-dns
      systemd:
        name: spark-dns
        state: restarted

    - name: Save iptables rules
      community.general.iptables_state:
        state: saved
        path: /etc/iptables/rules.v4
      # `iptables_state` needs the collection; fall back to the command the package ships with.
      failed_when: false
      register: _saved

    - name: Save iptables rules (fallback)
      shell: iptables-save > /etc/iptables/rules.v4
      when: _saved is failed or _saved is skipped
      changed_when: true
