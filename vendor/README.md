# vendor/

`netstack-smoltcp` is vendored here at **M0** (git submodule or source copy), pinned to a
0.2.x release, and depended on by `path` from `core/`. See `docs/PLAN.md` M0 and
`docs/STATE.md` for the exact step. Do not depend on it from crates.io.
