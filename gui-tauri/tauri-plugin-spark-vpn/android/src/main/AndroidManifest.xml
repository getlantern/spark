<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by ConnectivityManager.registerDefaultNetworkCallback (the reconnect watcher). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The split-tunnel "exclude apps" picker enumerates *launchable* apps only. On Android 11+
         other packages are hidden by default; this <queries> scoped to the launcher intent makes
         exactly that set visible (queryIntentActivities + loadLabel/loadIcon) — avoiding the
         policy-restricted QUERY_ALL_PACKAGES permission (which risks Play review rejection). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application>
        <!-- The VpnService must require BIND_VPN_SERVICE and advertise the VpnService action.
             It runs in the foreground (foregroundServiceType=specialUse + the "vpn" subtype) so
             Android keeps the tunnel alive while the app is backgrounded. It runs in a private
             ":vpn" process (android:process) so the UI process's WebView can be reclaimed while the
             tunnel stays up; the UI drives it over a control Messenger (SparkControlClient). This
             manifest merges into the consuming app. -->
        <service
            android:name="org.getlantern.spark.SparkVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:process=":vpn"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>
    </application>
</manifest>
